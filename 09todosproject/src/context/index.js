import { TodoContext,useTodo,TodoProvider } from "./TodoContext";

// here when we have multiple context we import here and use them thaats it
export {TodoContext,useTodo,TodoProvider} from './TodoContext'